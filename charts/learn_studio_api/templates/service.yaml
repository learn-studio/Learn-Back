apiVersion: v1
kind: Service
metadata:
  name: {{ template "learn_studio_api.fullname" . }}
  labels:
    app: {{ template "learn_studio_api.name" . }}
    chart: {{ template "learn_studio_api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  {{- if .Values.ingress.enabled }}
  type: ClusterIP
  {{- else }}
  type: LoadBalancer
  {{- end }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "learn_studio_api.name" . }}
    release: {{ .Release.Name }}
